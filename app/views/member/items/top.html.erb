<div class="container-fluid">
  <div class='row'>
    <div class='col-md-3'>
      <table class="table table-bordered mx-2 mt-5">
      <%= render 'member/items/genretable', genres: @genres %>
      </table>
    </div>
    <div class='col-md-8 offset-md-1'>
      <div class="row">
        <div class="col-md-6 border border-2 p-3 m-5">
          ようこそ、ながのCAKEへ！</br>
          このサイトは、ケーキ販売のECサイトになります。</br>
          会員ではない方も商品の閲覧はできますが、</br>
          購入には会員登録が必要になります。
        </div>
      </div>
      <h3 class="row m-4">
        新着商品
      </h3>
      <div class="row">
        <div class="d-flex justify-content-between">
          <% @items.each do |item| %>
            <div class="m-3">
              <%=link_to  member_item_path(item) do %>
                <%= attachment_image_tag item, :item_image ,size: '170x170'%>
              <% end %>
              <p><%= item.name %></p>
              <p>¥<%= item.no_tax_price %></p>
            </div>
          <% end %>
        </div>
      </div>
      <div class="float-right mr-4"><%= link_to 'すべての商品を見る>', member_items_path %></div>
    </div>
  </div>
</div>